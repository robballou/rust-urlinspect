[package]
name = "urlinspect"
version = "0.1.0"
edition = "2024"
description = "Simple CLI tool to output and decode URLs"

[dependencies]
url = "2.5.8"
serde = {version = "1.0", features = ["derive"]}
serde_yml = "0.0.12"
